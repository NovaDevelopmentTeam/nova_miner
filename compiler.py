import py-to-exe

filename = "path/to/python/script" # Replace "path/to/python/script" with the actual path to your Python script.
py-to-exe.convert_to("exe", filename)

# After successful conversion, you'll find the executable in the same directory as the script.